import { errorLog } from 'craigslist/helpers/error-log';
import { module, test } from 'qunit';

module('Unit | Helper | error log');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = errorLog([42]);
  assert.ok(result);
});
